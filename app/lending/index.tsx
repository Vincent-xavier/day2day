import { useRouter, type Href } from 'expo-router';
import { useMemo, useState } from 'react';
import { Pressable, ScrollView, Text, View } from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import { useLendingItems } from '@/db/hooks';
import { BottomBar, BottomSheet, Card, Button, EmptyState, ErrorState, LoadingState, Screen, SearchBar, styles } from '@/design-system';
import { formatMoney as money } from '@/utils/money';

type ViewMode = 'debtors' | 'requests' | 'offers';
type StatusFilter = 'open' | 'overdue' | 'settled' | 'all';
type Sort = 'recent' | 'due' | 'amount';

export default function LendingScreen() {
  const router = useRouter();
  const { data = [], isLoading, isError, refetch } = useLendingItems();
  const [mode, setMode] = useState<ViewMode>('debtors');
  const [status, setStatus] = useState<StatusFilter>('open');
  const [sort, setSort] = useState<Sort>('recent');
  const [search, setSearch] = useState('');
  const [filterOpen, setFilterOpen] = useState(false);
  const today = new Date().toISOString().slice(0, 10);
  const openItems = data.filter((item) => item.status === 'active' || item.status === 'overdue');
  const youGive = openItems.filter((item) => item.direction === 'lent').reduce((sum, item) => sum + Math.max((item.amountMinor ?? 0) - item.paidMinor, 0), 0);
  const youGet = openItems.filter((item) => item.direction === 'borrowed').reduce((sum, item) => sum + Math.max((item.amountMinor ?? 0) - item.paidMinor, 0), 0);
  const overdueCount = openItems.filter((item) => item.dueAt && item.dueAt < today).length;
  const visible = useMemo(() => data.filter((item) => {
    const modeMatch = mode === 'offers' || (mode === 'debtors' ? item.direction === 'lent' : item.direction === 'borrowed');
    const statusMatch = status === 'all' || (status === 'open' ? item.status === 'active' || item.status === 'overdue' : status === 'overdue' ? item.status === 'overdue' || (item.status === 'active' && !!item.dueAt && item.dueAt < today) : item.status === 'returned');
    const query = search.trim().toLowerCase();
    return modeMatch && statusMatch && (!query || `${item.personName} ${item.name} ${item.description ?? ''}`.toLowerCase().includes(query));
  }).sort((a, b) => sort === 'amount' ? (b.amountMinor ?? 0) - (a.amountMinor ?? 0) : sort === 'due' ? (a.dueAt ?? '9999-12-31').localeCompare(b.dueAt ?? '9999-12-31') : b.createdAt.localeCompare(a.createdAt)), [data, mode, search, sort, status, today]);
  const openDetails = (itemId: string) => router.push(`/lending/${itemId}` as Href);

  return <Screen><ScrollView contentContainerStyle={{ paddingBottom: 18 }}><LinearGradient colors={['#3b3178', '#6d5efc']} style={styles.lendingHero}><View style={styles.listHeader}><View><Text style={styles.lendingEyebrow}>LENDING</Text><Text style={styles.lendingTitle}>People & balances</Text></View><Pressable accessibilityRole="button" accessibilityLabel="Add ledger entry" onPress={() => router.push('/lending/new')} style={styles.lendingAdd}><Text style={styles.lendingAddText}>＋</Text></Pressable></View><View style={styles.lendingHeroRow}><View><Text style={styles.lendingCaption}>Your open balance</Text><Text style={styles.lendingAmount}>{money(youGet - youGive)}</Text></View><View style={styles.lendingHeroMeta}><Text style={styles.lendingCaption}>Overdue</Text><Text style={styles.lendingMetaValue}>{overdueCount}</Text></View></View></LinearGradient><View style={styles.lendingTabs}>{([{ key: 'debtors', label: 'Debtors' }, { key: 'requests', label: 'Requests' }, { key: 'offers', label: 'My offers' }] as const).map((tab) => <Pressable key={tab.key} accessibilityRole="tab" accessibilityState={{ selected: mode === tab.key }} onPress={() => setMode(tab.key)} style={[styles.lendingTab, mode === tab.key && styles.lendingTabActive]}><Text style={[styles.lendingTabText, mode === tab.key && styles.lendingTabTextActive]}>{tab.label}</Text></Pressable>)}</View><View style={styles.lendingToolbar}><Text style={styles.sectionLabel}>{visible.length} ENTRIES</Text><Pressable accessibilityRole="button" accessibilityLabel="Customize ledger filters" onPress={() => setFilterOpen(true)}><Text style={styles.linkText}>Filter & sort</Text></Pressable></View><SearchBar value={search} onChangeText={setSearch} placeholder="Search contacts or entries" /><View style={styles.row}><Card style={styles.stat}><Text style={styles.muted}>You give</Text><Text style={[styles.statValue, { color: '#fda4af' }]}>{money(youGive)}</Text></Card><Card style={styles.stat}><Text style={styles.muted}>You get</Text><Text style={[styles.statValue, { color: '#72dfad' }]}>{money(youGet)}</Text></Card></View>{isError ? <ErrorState message="Your ledgers couldn't load." onRetry={() => refetch()} /> : isLoading ? <LoadingState label="Loading your ledgers..." /> : visible.length === 0 ? <EmptyState title="No entries here" message="Add a ledger entry to start tracking this view." action="Add ledger entry" onAction={() => router.push('/lending/new')} /> : visible.map((item) => { const overdue = item.status !== 'returned' && !!item.dueAt && item.dueAt < today; const remaining = Math.max((item.amountMinor ?? 0) - item.paidMinor, 0); return <Card key={item.id} style={styles.lendingCard}><View style={styles.listHeader}><View style={styles.contactIdentity}><View style={styles.contactAvatar}><Text style={styles.contactAvatarText}>{item.personName[0]?.toUpperCase() ?? '?'}</Text></View><View style={{ flex: 1 }}><Text style={styles.heading}>{item.personName}</Text><Text style={styles.muted}>{item.name} · {item.direction === 'lent' ? 'You give' : 'You get'}</Text></View></View>{item.amountMinor ? <Text style={styles.lendingCardAmount}>{money(remaining)} left</Text> : null}</View><View style={styles.lendingMetaRow}><Text style={overdue ? styles.destructiveText : styles.muted}>{overdue ? 'Overdue' : item.status === 'returned' ? 'Settled' : item.dueAt ? `Due ${item.dueAt}` : 'No due date'}</Text><Text style={styles.muted}>{item.paidMinor ? `Paid ${money(item.paidMinor)}` : 'No payment yet'}</Text></View><Button title="View details & payments" variant="secondary" onPress={() => openDetails(item.id)} /></Card>; })}</ScrollView><BottomSheet visible={filterOpen} title="Customize ledger view" onClose={() => setFilterOpen(false)}><Text style={[styles.muted, { marginBottom: 8 }]}>Status</Text><Button title="Open balances" variant={status === 'open' ? 'primary' : 'secondary'} onPress={() => { setStatus('open'); setFilterOpen(false); }} /><Button title="Overdue balances" variant={status === 'overdue' ? 'primary' : 'secondary'} onPress={() => { setStatus('overdue'); setFilterOpen(false); }} /><Button title="Settled balances" variant={status === 'settled' ? 'primary' : 'secondary'} onPress={() => { setStatus('settled'); setFilterOpen(false); }} /><Button title="All balances" variant={status === 'all' ? 'primary' : 'secondary'} onPress={() => { setStatus('all'); setFilterOpen(false); }} /><Text style={[styles.muted, { marginTop: 14, marginBottom: 8 }]}>Sort by</Text><Button title="Most recent" variant={sort === 'recent' ? 'primary' : 'secondary'} onPress={() => setSort('recent')} /><Button title="Due date" variant={sort === 'due' ? 'primary' : 'secondary'} onPress={() => setSort('due')} /><Button title="Largest amount" variant={sort === 'amount' ? 'primary' : 'secondary'} onPress={() => setSort('amount')} /></BottomSheet><BottomBar active="lending" onNavigate={(tab) => { if (tab === 'home') router.push('/dashboard'); if (tab === 'money') router.push('/transactions'); if (tab === 'lending') router.push('/lending'); if (tab === 'tasks') router.push('/tasks'); if (tab === 'settings') router.push('/settings' as Href); }} /></Screen>;
}
